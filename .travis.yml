language: node_js
node_js:
  - "8"
branches:
  only:
    - develop
    - master
addons:
  sonarcloud:
    organization: "wslyvh-github"
    token:
      secure: ${SONAR_TOKEN}
before_install:
  - npm update
install:
  - npm install
  - npm install codecov -g
script:
  - npm run lint
  - npm run test
  - npm run doc
after_success:
  - codecov
  - sonar-scanner
deploy:
  provider: pages
  skip_cleanup: true
  github_token: ${GITHUB_TOKEN}
  local_dir: "docs"
  on:
    branch: master
before_deploy:
  - touch .nojekyll
after_deploy:
  - touch .nojekyll
